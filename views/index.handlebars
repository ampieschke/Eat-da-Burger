<h1>Burgers!</h1>

<h2>Burgers for Eatin'!</h2>

<ul>
  {{#each cats}}
  {{#unless devoured}}
  {{> burgers/cat-block devoured=true}}
  {{/unless}}
  {{/each}}
</ul>

<h2>Burgers that got Eaten!</h2>

<ul>
  {{#each burger}}
  {{#if devoured}}
  {{> cats/cat-block sleep=false}}
  {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form id="create-form">

  <div class="form-group">
    <label for="bu">Burger Name:</label>
    <input type="text" id="bu" name="name">
  </div>

  <div class="form-group">
    <label for="sleepy">Did you eat it yet?</label><br>
    <input type="radio" name="sleepy" value="1" id="sleepy" checked> Yes, I ate it!<br>
    <input type="radio" name="sleepy" value="0" id="awake"> NO, not yet.
  </div>

  <button type="submit">Add Burg</button>
</form>